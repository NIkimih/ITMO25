git init - создание репозитория

далее необходимо его настроить:
git config --global user.name "имя"
git config --global user.email "емайл"
git config --global core.autoclrf true  // mac/linux
git config --global core.autoclrf input // mac/linux
git config --global core.safecrlf true  // mac/linux


git add "имя_файла"
git add .   -   добавление всех видимых файлов
git ignore  -   файлы, окторые гит не отслежвиает
git status  -   статус файлов, которые гит отслежвиает
git commit -m "комментарий"  -   сделать комментарий
git clone ""    -   склонировтаь/скачать репозиторий
git remote add ""   -   куда будет отправляться копия локлаьного репозитория
git push "репозиторий ветка"    -   Отправка локальных слепков в удаленный репозиторий, например гитхаб.
git pull "репозиторий ветка"    -   Получение слепков из ветки на локальный репозиторий.

git cheсkout -b "name" - создаем ветку
git push - заливаем на гит хаб (дальее мержим на гитхабе)




Концепция DOM

CSS

id - 1 эл-т только
class - может быть присвоен нескольким эл-ам
методолгия БЭМ
селектор со значением !important - если писать с этим значением, то стиль нельзя будет переопреить.
специфичность для стилей - почитать (может пригодитсья на собесе)
псевдоселекторы - обращаются к состоянию эл-та

 Позиционирование:
 relative - относительно от места расположения элемента
 absolute - абсолютное от ближайшего элемента с position: "не статик"
 fixed - фиксированое относительно окна браузера
 stickey - прикольное св-во
 сво-во стикет (???) - почитать 
 эл-ты с position не статик можно смещать относительно РОДИТЕЛЯ


 псевдоэлементы:
::before
::after
псевдоклассы:


css переменные - пользовательские свойства, которые начинаются с --"name"
использование переменной: color: var(--"name");

:root{} - это область, откуда переменные доступын для всего файла.
